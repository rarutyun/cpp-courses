// What is a compilation error?
// Сompile and show compilation error
// Fix error(order is important)
// Show assembler code (use -S)

#include "00_01_00_compilation_error.h"
int a = 5;

int main() {
    int number = my_func();

    return 0;
}
