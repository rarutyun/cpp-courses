// Implement std::is_same and std::is_same_v

#include <iostream>

namespace my {
    struct is_same {};
}

int main() {
    // std::cout << my::is_same<int, int>::value << std::endl;
    // std::cout << my::is_same_v<int, double> << std::endl;
}
